import {EmailPrivacyPipe} from './email-privacy.pipe';

describe('EmailPrivacyPipe', () => {
  it('create an instance', () => {
    const pipe = new EmailPrivacyPipe();
    expect(pipe).toBeTruthy();
  });
});
